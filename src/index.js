import "./style.css";
import { createproject } from "./storage";
import { makeProjectBtn } from "./display";

// * Create a list of projects each button loads the project info
makeProjectBtn();

// * button to create projects
createproject();
